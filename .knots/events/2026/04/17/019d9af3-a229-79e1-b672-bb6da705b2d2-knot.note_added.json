{
  "event_id": "019d9af3-a229-79e1-b672-bb6da705b2d2",
  "occurred_at": "2026-04-17T10:19:11.273689Z",
  "knot_id": "foolery-2782",
  "type": "knot.note_added",
  "data": {
    "agentname": "unknown",
    "content": "## Handoff Capsule — SHIPPED\n\n### Root cause\nThe interactive session watchdog in src/lib/agent-session-runtime-helpers.ts fired silently after `config.watchdogTimeoutMs` of inactivity. The setTimeout callback called `terminateProcessGroup(child)` with no console output and no lifecycle event. The resulting SIGTERM looked identical to a graceful process exit in logs — a `child_close code=0 aborted=false` with no breadcrumb. That silence caused operators investigating knot foolery-557d iteration 3 (abrupt ~10 min termination around 2026-04-17 07:34:09) to waste time looking for crashes when the real cause was the watchdog.\n\n### Fix\nBefore calling `terminateProcessGroup`, the watchdog timeout callback now:\n1. Emits `console.warn(\"[terminal-manager] [watchdog] timeout_fired pid=<pid> timeoutMs=<ms> msSinceLastEvent=<delta> lastEventType=<type|null> reason=timeout\")`.\n2. Invokes `config.onLifecycleEvent?.({ type: \"watchdog_fired\", timeoutMs, msSinceLastEvent, lastEventType })`.\n\n### Files touched\n- `src/lib/agent-session-runtime.ts` — added `watchdog_fired` variant to `SessionRuntimeLifecycleEvent` union (lines ~83-89); added `watchdogArmedAt: number | null` to `SessionRuntimeState` (line ~137); initialized `watchdogArmedAt: null` in `createSessionRuntime` (line ~422).\n- `src/lib/agent-session-runtime-helpers.ts` — added new `fireWatchdogTimeout(child, state, config, ms)` function (lines ~275-305) that performs the log+event emission and then terminates; updated `doResetWatchdog` (lines ~307-321) to stamp `state.watchdogArmedAt = Date.now()` before arming the timer and to delegate firing to `fireWatchdogTimeout`.\n- `src/lib/__tests__/agent-session-runtime-watchdog-fired.test.ts` — new unit test file with one test `emits watchdog_fired and warns before SIGTERM` using `vi.useFakeTimers()`, spying on `console.warn` and `process.kill`, asserting the warn message contains the required tag/fields, the lifecycle event is emitted with correct values, and the warn happens before SIGTERM via `invocationCallOrder`.\n\n### Test that locks this down\n`src/lib/__tests__/agent-session-runtime-watchdog-fired.test.ts::runtime: watchdog_fired lifecycle::warns and emits event before SIGTERM`. If this test is ever removed or weakened the fix will silently regress; this is the canary.\n\n### Why this won't regress\n- The test asserts the exact warn tag `[terminal-manager] [watchdog]`, timeout_fired substring, pid=12345, timeoutMs=10000, reason=timeout, and lastEventType=assistant.\n- The test asserts `warnSpy.mock.invocationCallOrder[0] < killSpy.mock.invocationCallOrder[0]`, so any refactor that moves the terminate call ahead of the warn fails loudly.\n- The type system guarantees the `watchdog_fired` union variant — anyone deleting the variant in `SessionRuntimeLifecycleEvent` will hit a TS error at the callsite in `fireWatchdogTimeout`.\n\n### Reference commit hashes\n- Implementation: c1704a95 (fix(foolery-2782): log watchdog firing before SIGTERM)\n- Merge on main: 1a99ec1c (Merge worktree-foolery-2782)\n\n### Out-of-scope boundaries respected\n- Did NOT touch onResult / handleResultEvent (knot foolery-a401).\n- Did NOT touch terminateProcessGroup body (knot foolery-062f).\n- Did NOT add msSinceLastStdout to child_close logs (knot foolery-e750).\n- Did NOT remove the `fake-fixed-already` tag; preserved per instructions.",
    "datetime": "2026-04-17T10:19:11.273689Z",
    "entry_id": "019d9af3-a229-79e1-b672-bb567a2c5ec8",
    "model": "unknown",
    "username": "unknown",
    "version": "unknown"
  }
}
