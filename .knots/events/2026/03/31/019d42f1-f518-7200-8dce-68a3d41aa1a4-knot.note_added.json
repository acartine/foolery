{
  "event_id": "019d42f1-f518-7200-8dce-68a3d41aa1a4",
  "occurred_at": "2026-03-31T08:10:46.424245Z",
  "knot_id": "foolery-20ee",
  "type": "knot.note_added",
  "data": {
    "agentname": "Codex",
    "content": "Implementation plan:\n1. Replace the take-loop's single-agent retry memory with queue-type failure history in [src/lib/terminal-manager-take-loop.ts]. Specifically, extend TakeLoopContext with per-queue-type failed-agent tracking (agentId-based, because pool membership and exclusion are keyed by agentId) while keeping lastAgentPerQueueType only as a soft exclusion for ordinary non-failure rotation. This lets the loop remember every agent that has already failed for a queue type in the current session instead of only the most recent one.\n2. Rework retry selection in [src/lib/terminal-manager-take-agent.ts] so failed-claim rotation uses that accumulated failure set for both step-failure rollback and non-zero-exit retry. The retry path should consult the step pool regardless of dispatchMode, exclude every previously failed pooled agent for that queue type, and stop early when the remaining eligible pool is empty instead of reusing a failed agent until maxClaimsPerQueueType is exhausted. Ordinary non-retry selection can continue using soft exclusion from lastAgentPerQueueType / review exclusion.\n3. Tighten pool APIs in [src/lib/agent-pool.ts] so exclusion is explicit and never silently defeated. selectFromPool should stop falling back to an excluded agent; either it returns a non-excluded candidate or null when exclusion removes the entire valid pool. If helpful, add a multi-exclusion helper (for example excludeAgentIds: Set<string>) and have strict/error-retry callers use that directly, so the take loop can distinguish between “no pool configured”, “pool has alternatives”, and “pool exhausted by prior failures”.\n4. Update failure handling in [src/lib/terminal-manager-take-iteration.ts] and claim finalization/reset points so a non-zero exit records the just-failed agent into the queue-type failure history before the next retry is built, while a successful claim/iteration resets or narrows that history appropriately when the loop advances to a fresh queue-type attempt. This is where the current advanced-only error-retry gate should be removed from behavior, not just papered over in tests.\n5. Expand regression coverage. In [src/lib/__tests__/terminal-manager-queue-claims.test.ts], add scenarios proving that repeated failures rotate through all available pool agents before stopping, that basic dispatch mode still rotates on failure when a step pool exists, and that the loop stops early once every pool agent has failed instead of burning the rest of maxClaimsPerQueueType. In [src/lib/__tests__/agent-pool.test.ts] / [src/lib/__tests__/agent-pool-strict.test.ts], update expectations so exclusion returns null instead of silently selecting the excluded agent and add coverage for multi-failure exclusion behavior.\n6. Verification target for implementation: run the focused Vitest suites around queue claims and agent-pool selection first, then the repo quality gates required by AGENTS.md if code changes are made in the later implementation step.\n\nRisks / decisions:\n- Exclusion identity should be agentId-based, not model-name-based, because pool configuration, bookkeeping, and acceptance criteria all describe rotating across pool agents. If multiple entries point at the same underlying model, they still count as distinct pool agents unless the implementation later adds a separate effective-target de-duplication policy.\n- Basic-mode behavior should remain unchanged for normal claim selection; the broadened pool retry logic only applies after a failure when the system is explicitly trying to avoid rerunning the same agent.",
    "datetime": "2026-03-31T08:10:46.424245Z",
    "entry_id": "019d42f1-f518-7200-8dce-68999f3675b7",
    "model": "gpt",
    "username": "unknown",
    "version": "5.4"
  }
}
