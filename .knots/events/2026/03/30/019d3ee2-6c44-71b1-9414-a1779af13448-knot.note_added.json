{
  "event_id": "019d3ee2-6c44-71b1-9414-a1779af13448",
  "occurred_at": "2026-03-30T13:15:19.492051Z",
  "knot_id": "foolery-23f3",
  "type": "knot.note_added",
  "data": {
    "agentname": "Claude",
    "content": "# Revised Implementation Plan v2: Streamline Scanned-Agent Rows and Button Styling\n\n## Review Feedback Addressed\n\n1. **Replacement add control**: The per-row Add and Add All buttons are replaced by a single section-level \"Add Selected\" button in the ScannedAgentsList header. It imports all multi-selected models across all agents in one action.\n2. **Codex rename scope**: Explicit list of all provider-id consumers beyond agent-identity.ts.\n\n---\n\n## 1. Rename OpenAI → Codex (`src/lib/agent-identity.ts`)\n\n### Type-level change\n- `AgentProviderId` union: `\"openai\"` → `\"codex\"` (line 6)\n- `PROVIDER_LABELS`: key `openai: \"OpenAI\"` → `codex: \"Codex\"` (line 32)\n\n### Detection logic\n- `detectAgentProviderId()` (line 78): return `\"codex\"` instead of `\"openai\"` for codex/chatgpt/openai commands\n\n### Display label changes\n- `providerLabel()` (line 84): No change needed — it reads PROVIDER_LABELS dynamically.\n- `normalizeAgentIdentity()` (line 251): Change `provider === \"OpenAI\"` guard to `provider === \"Codex\"`. This branch calls `normalizeCodexModel` — function name already correct.\n- `normalizeCopilotModel()` (line 182): Change `provider: \"OpenAI\"` return to `provider: \"Codex\"` for GPT/Codex sub-models routed through Copilot.\n- `formatAgentFamily()` (line 330): Change `provider === \"OpenAI\"` guard to `provider === \"Codex\"`.\n\n### Consumers outside agent-identity.ts (explicit scope)\n\n| File | What to change |\n|------|---------------|\n| `src/components/agent-info-bar.tsx` line 19 | `VENDOR_CONFIG` key `openai` → `codex` |\n| `src/hooks/use-agent-info.ts` line 23 | `vendor` field comment: cosmetic only, `detectVendor` returns the new `\"codex\"` id automatically |\n| `src/components/__tests__/lease-audit-view-helpers.test.ts` | Uses `\"openai\"` as raw provider string in test data fixtures (e.g. line 95). These are stored data values, NOT the AgentProviderId type — **leave unchanged** unless the data normalization pipeline also renames stored values (it doesn't). |\n| `src/lib/__tests__/agent-identity.test.ts` | Update test expectations for `detectAgentProviderId` calls that expect `\"openai\"` to now expect `\"codex\"`. |\n\n---\n\n## 2. State Shape Change (`src/components/settings-agents-hooks.tsx`)\n\n### `useAgentScanner` (line 51)\n- Change `selectedScannedOptions` type: `Record<string, string>` → `Record<string, string[]>`\n- Initialize with arrays: `agent.selectedOptionId ?? agent.options?.[0]?.id` wrapped in `[...]`\n- Return type updates accordingly.\n\n### `resolveSelectedOption` → `resolveSelectedOptions` (`src/components/settings-agents-scanned.tsx` line 57)\n- New signature: `(scanned: ScannedAgent, selectedOptions: Record<string, string[]>) → ScannedAgentOption[]`\n- Returns array of resolved options matching the selected IDs.\n- Keep a compat helper `resolveFirstSelectedOption` for any callers that need a single option.\n\n### `useAgentMutations` (line 160)\n- `handleAddScanned` (line 166): Accepts a list of `{agent, options}` tuples. Loops over each, calling `addAgent()` per option. Sets first added as default.\n- `handleAddAll` (line 201): **Remove entirely** — replaced by the same `handleAddScanned` accepting multi-agent input.\n- `addAllAgents` helper (line 112): Refactor to accept multi-select shape — loop agents × selected options.\n- `buildAgentPayload` (line 35): Unchanged — still builds payload from one `(scanned, selected)` pair.\n\n---\n\n## 3. ScannedAgentsList UI Changes (`src/components/settings-agents-scanned.tsx`)\n\n### 3a. Remove Add All button (lines 114-124)\n- Remove the `onAddAll` prop entirely.\n- Remove the `unregisteredInstalled` computation (lines 96-105) — no longer needed for button visibility.\n\n### 3b. Add section-level \"Add Selected\" button\n- Place in the header row (where Add All was), visible when any model is selected across any unregistered agent.\n- Styled with `variant=\"success\"` (emerald-600 bg, emerald-300 border) per button color spec.\n- On click: calls new `onAddSelected()` prop which collects all selected models across all agents and imports them.\n- Label: \"Add Selected\" with Plus icon, or \"Add Selected (N)\" showing count.\n\n### 3c. Remove per-row Add button and registered pill (lines 322-339)\n- Remove `onAdd` prop from `ScannedAgentRow`.\n- Remove `isRegistered` prop from `ScannedAgentRow`.\n- Remove both conditional blocks (Add button lines 322-331, registered badge lines 332-339).\n\n### 3d. Show full CLI path under agent name (lines 305-312)\n- Replace the truncated Badge with a plain `<span>` subtitle:\n  ```tsx\n  <span className=\"text-[10px] text-muted-foreground truncate\" title={agent.path}>\n    {agent.path}\n  </span>\n  ```\n- Keep the \"not found\" badge for `!agent.installed`.\n- Layout: stack agent name and path vertically in a `flex-col` container.\n\n### 3e. Remove agent.id span (line 363)\n- Delete `<span className=\"shrink-0\">{agent.id}</span>` from `renderOptionSelector`.\n\n### 3f. Convert to multi-select with pill badges\n- Replace both `SearchableOptionCombobox` and `<Select>` with a unified multi-select combobox.\n- **MultiSelectModelCombobox** (new internal component, same file):\n  - Uses Popover + Command pattern (already imported).\n  - Props: `options`, `displayMap`, `values: string[]`, `onToggle: (id: string) => void`.\n  - Toggle behavior: clicking an item adds/removes from selection; popover stays open.\n  - Trigger shows count: \"N model(s) selected\" or \"select models\".\n  - Check icon shows for selected items (opacity toggle, same pattern as existing).\n- **Selected model pills**: Render to the right of the row (in the space formerly occupied by Add/registered).\n  - Use `<Badge variant=\"secondary\">` for each selected model.\n  - Display text from `displayMap`.\n  - Layout: `flex flex-wrap gap-1` in the right section of the row header.\n\n### 3g. Row layout restructure\nCurrent: `[name + path] ... [Add/registered]`\nNew: `[name (stacked with path)] ... [selected model pills]`\nBelow: `[multi-select dropdown]`\n\n---\n\n## 4. Section Wiring (`src/components/settings-agents-section.tsx`)\n\n### Props update (lines 66-81)\n- Remove `onAdd` and `onAddAll` props from `<ScannedAgentsList>`.\n- Add `onAddSelected` prop: collects all selected options, calls refactored `handleAddScanned`.\n- Change `onSelectOption` callback to toggle arrays:\n  ```tsx\n  onSelectOption={(agentId, optId) =>\n    setSelectedScannedOptions((p) => {\n      const current = p[agentId] ?? [];\n      const next = current.includes(optId)\n        ? current.filter(id => id !== optId)\n        : [...current, optId];\n      return { ...p, [agentId]: next };\n    })\n  }\n  ```\n\n### Button styling (lines 189-208)\n- Scan button (line 189): Change to `variant=\"success-light\"` (matching \"Create More\" button).\n- Add button (line 199): Change to `variant=\"success\"` (emerald-600 dark-green).\n\n---\n\n## 5. Test Updates\n\n| File | Changes |\n|------|---------|\n| `src/lib/__tests__/agent-identity.test.ts` | Update `detectAgentProviderId` expectations: `\"openai\"` → `\"codex\"`. Update `normalizeAgentIdentity` tests for `provider: \"Codex\"`. Update `formatAgentFamily` tests for `\"Codex\"` provider. |\n| `src/components/__tests__/settings-agents-scanned.test.ts` | `filterSearchableOption` tests — likely no changes needed. |\n| `src/components/__tests__/lease-audit-view-helpers.test.ts` | **No changes** — uses raw provider strings from data, not the AgentProviderId type. |\n\n---\n\n## Execution Order\n\n1. **agent-identity.ts + agent-info-bar.tsx**: Rename openai → codex in type, labels, detection, normalizer, vendor config. Update tests.\n2. **settings-agents-hooks.tsx**: Change state shape to `Record<string, string[]>`, refactor mutation handlers for multi-select.\n3. **settings-agents-scanned.tsx**: Remove Add/registered/Add All, add CLI path subtitle, remove agent.id span, build MultiSelectModelCombobox, add pill badges, add \"Add Selected\" button.\n4. **settings-agents-section.tsx**: Wire new props (onAddSelected, toggle onSelectOption), update button variants.\n5. **Quality gates**: `bun run lint && bunx tsc --noEmit && bun run test && bun run build`\n\n## Complexity: Medium\n~6 files changed, ~250 lines modified. Main complexity is the multi-select combobox + section-level add wiring.\n\n## Risks\n- **Multi-select popover**: Must stay open on item click (override default Command close behavior). Mitigation: the existing SearchableOptionCombobox already controls `open` state manually.\n- **Batch add errors**: Adding N models sequentially could partially fail. Mitigation: existing `addAllAgents` already handles sequential adds with early-exit on error.\n- **Codex rename in stored data**: The rename only affects the UI type system and display labels. Stored `provider` strings in settings.json may still say \"OpenAI\" from prior scans — `providerLabel()` already normalizes via command-based detection, so display is correct regardless of stored value.",
    "datetime": "2026-03-30T13:15:19.492051Z",
    "entry_id": "019d3ee2-6c44-71b1-9414-a16e1901108a",
    "model": "opus/claude",
    "username": "unknown",
    "version": "4.6"
  }
}
